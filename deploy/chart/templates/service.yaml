apiVersion: v1
kind: Service
metadata:
  name: {{ include "augur.fullname" . }}
  labels: {{- include "augur.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
  selector: {{- include "augur.selectorLabels" . | nindent 4 }}
